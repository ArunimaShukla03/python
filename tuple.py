coordinates = (4, 5)

#tuples are immutable, i.e., once you create a tuple you cannot add or change any element inside it

#the following code will show error

#coordinates[1]=10
#print(coordinates[1])

polar=(5,4)
print(polar[0])